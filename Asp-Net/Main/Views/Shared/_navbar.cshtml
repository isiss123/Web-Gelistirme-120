<nav>
    <div class="navbar navbar-expand-md navbar-dark bg-primary">
        <div class="container">
            <a href="/" class="navbar-brand">AxotApp</a>
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a asp-controller="user" asp-action="list" class="nav-link">List</a>
                </li>
                @if(User.Identity.IsAuthenticated)
                {
                    <li class="nav-item">
                        <a class="nav-link" href="#">Cart</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Orders</a>
                    </li>
                    @if(User.IsInRole("Satici") || User.IsInRole("Mod"))
                    {
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="productlist" class="nav-link">Admin Products</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="categorylist" class="nav-link">Admin Categories</a>
                        </li>
                    }
                    @if(User.IsInRole("Admin"))
                    {
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="productlist" class="nav-link">Admin Products</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="categorylist" class="nav-link">Admin Categories</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="rolelist" class="nav-link">Admin Roles</a>
                        </li>
                    }
                    
                    
                }
            </ul>
            <ul class="navbar-nav ml-auto">
                @if(User.Identity.IsAuthenticated)
                {
                    <li class="nav-item">
                        <a href="/account/logout" class="nav-link">logout</a>
                    </li>
                    <li class="nav-item">
                        <a href="/account/manage" class="nav-link">@User.Identity.Name</a>
                    </li>
                }else{
                    <li class="nav-item">
                        <a class="nav-link" href="/account/login">login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/account/register">register</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>