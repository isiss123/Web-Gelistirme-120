<nav>
    <div class="navbar navbar-expand-md navbar-dark bg-primary">
        <div class="container">
            <a href="/" class="navbar-brand">AxotApp</a>
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a asp-controller="user" asp-action="list" class="nav-link">List</a>
                </li>
                @if(User.Identity.IsAuthenticated)
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="cart" asp-action="index">Cart</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Orders</a>
                    </li>
                    @if(User.IsInRole("satici") || User.IsInRole("mod"))
                    {
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="productlist" class="nav-link">Products</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="categorylist" class="nav-link">Categories</a>
                        </li>
                    }
                    @if(User.IsInRole("admin"))
                    {
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="productlist" class="nav-link">Products</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="categorylist" class="nav-link">Categories</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="rolelist" class="nav-link">Roles</a>
                        </li>
                        <li class="nav-item">
                            <a asp-controller="admin" asp-action="userlist" class="nav-link">Users</a>
                        </li>
                    }
                    
                    
                }
            </ul>
            <ul class="navbar-nav ml-auto">
                @if(User.Identity.IsAuthenticated)
                {
                    <li class="nav-item">
                        <a href="/account/logout" class="nav-link">logout</a>
                    </li>
                    <li class="nav-item">
                        <a href="/account/manage" class="nav-link">@User.Identity.Name</a>
                    </li>
                }else{
                    <li class="nav-item">
                        <a class="nav-link" href="/account/login">login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/account/register">register</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>